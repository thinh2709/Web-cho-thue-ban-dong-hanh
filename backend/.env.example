PORT=3001

# Auth (nếu bật route /api/auth): bắt buộc khi đăng ký/đăng nhập
# JWT_SECRET=thay-bang-chuoi-bi-mat-dai

# Dữ liệu lưu trong bộ nhớ (RAM) — khởi động lại server là mất thay đổi (trừ favorites.json trên đĩa).
