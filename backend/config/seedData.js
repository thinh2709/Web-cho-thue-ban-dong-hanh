/**
 * Trước đây seed MongoDB. Ứng dụng dùng bộ nhớ trong — dữ liệu mẫu nằm trong `src/db/memoryEngine.js` (bootstrap).
 */
export async function seedDatabase() {
  console.log("[seed] Bỏ qua — không còn MongoDB, dữ liệu mẫu load từ memoryEngine.");
}
